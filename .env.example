PORT=3000
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d
DATABASE_URL="file:./dev.db"
APP_URL=http://localhost:3000

# Optional — wire these in server/mailer.js when adding nodemailer
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
